{% extends 'base.html.twig' %}

{% block title %}ManuscriptRole{% endblock %}

 {% block pageheader %}
     <h1>ManuscriptRole Details</h1>
 {% endblock %} 

{% block body %}
    <div class="btn-toolbar">
        <div class="btn-group pull-right">
            <a href="{{ path('manuscript_role_index') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left"></span> Back </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('manuscript_role_edit', { 'id': manuscriptRole.id }) }}" class="btn btn-default">
                    <span class='glyphicon glyphicon-edit'></span> Edit </a>
                <a href="{{ path('manuscript_role_delete', { 'id': manuscriptRole.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this ManuscriptRole?">
                    <span class='glyphicon glyphicon-remove'></span> Delete </a>
            {% endif %}
        </div>
    </div>

    {% embed 'NinesUtilBundle:term/partial:show.html.twig' with {'term': manuscriptRole} %}
    {% endembed %}

    {% if manuscriptRole.contributions | length > 0 %}
        <h2>Contributions</h2>
        <ul>
            {% for contributions in manuscriptRole.contributions %}
                <li>
                    <a href="{{ path("contribution_show", {"id":contributions.id}) }}">
                        {{ contributions }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
