<!DOCTYPE html>

<h1>{{ term.label }}</h1>
<div class="cat-description">
    {{ term.description|raw }} 
</div>
{% if is_granted('ROLE_CONTENT_ADMIN') %}
<ul class="list-group-horizontal">
        <li><em>Created</em>: {% if term.created %}{{ term.created|date }}{% endif %}</li>
        <li><em>Updated</em>: {% if term.updated %}{{ term.updated|date }}{% endif %}</li>
</ul>
{% endif %}