{% extends 'base.html.twig' %}

{% block id %}index{% endblock %}
{% block hero %}
<div class="jumbotron">
    <div class="container">
        <h1 class="sr-only">Manuscript Verse Miscellanies</h1>
        <img src="{{ asset('images/title.svg') }}" alt="Manuscript Verse Miscellanies"/>
        {# TODO: Remove the conditional once homepage is defined in production #}
        {% if homepage is not null %}
            {% set firstParagraph = homepage.excerpt|split('</p>')|first  ~ '</p>' %}
            {{ firstParagraph |raw }}
            <a class="readMore" href="{{ path('nines_blog_page_show', { 'id': homepage.id }) }}">READ MORE</a>
        {% else %}
        <p>Lorem ipsum dolor sit amet, sumo enim dicit vix at, ius commodo debitis eu, an mei novum harum. Eum id dicunt corrumpit signiferumque, integre interesset vim in, modus vituperata ut vix. Et nostro scriptorem contentiones eos. Usu ludus ceteros vituperatoribus no, vix at paulo oportere, usu ea nusquam incorrupte. In lucilius disputando sea, duis dolores consetetur mea ea. In his labore recusabo reprimique.</p>
        {% endif %}
    </div>
    
</div>
{% endblock %}

{% block body %}
    <section>
        <h2>Browse by Archive</h2>
        <ul class="archive-gallery">
            {% for archive in archives %}
            <li>
                <a href="{{ path('archive_show', {'id':archive.id}) }}"><span class="name">{{ archive }}</span></a>
            </li>   
            {% endfor %}
        </ul>
    </section>
    <section>
        <h2>Browse by Person</h2>
        <input type="text" class="contributor" placeholder="Eleanor Peart"></input>
    </section>
<section>
    <h2>Browse by Period</h2>
    <ul class="period-list">
        {% for period in periods %}
            <li>
                <a href="{{ path('period_show', {'id':period.id})}}">{{ period|replace({'-':'​-​'}) }}</a>
            </li>
        {% endfor %}
    </ul>
</section>
{% endblock %}
