{% extends 'base.html.twig' %}

{% block title %}{{ period.label }}{% endblock %}

 {% block pageheader %}
    {% include 'partial/category-index.html.twig' with {'term': period} %}
 {% endblock %} 
 {% block breadcrumb %}
 {% include 'partial/breadcrumb.html.twig' with {'index': 'period', 'show': period.label} %}
 {% endblock %}

{% block admin %}
<div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('period_edit', { 'id': period.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit </a>
        <a href="{{ path('period_delete', { 'id': period.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this Period?">
            <span class='glyphicon glyphicon-remove'></span> Delete </a>
    </div>
</div>
{% endblock %}

{% block body %}

{% include 'manuscript/partial/gallery.html.twig' with {'manuscripts': period.manuscripts} %}
{% endblock %}
